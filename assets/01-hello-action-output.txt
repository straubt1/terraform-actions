=== SECTION 1: Default apply ===

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create

Terraform will perform the following actions:

  # random_pet.this will be created
  + resource "random_pet" "this" {
      + id        = (known after apply)
      + length    = 2
      + separator = "-"
    }

    # Actions to be invoked after this change in order:
    action "local_command" "hello" {
        config {
            arguments = [
                "Hello from Terraform Actions!",
            ]
            command   = "echo"
        }
    }


Plan: 1 to add, 0 to change, 0 to destroy. Actions: 1 to invoke.
random_pet.this: Creating...
random_pet.this: Creation complete after 0s [id=amusing-bedbug]
Action started: action.local_command.hello (triggered by random_pet.this)
Action action.local_command.hello (triggered by random_pet.this): 

Hello from Terraform Actions!


Action complete: action.local_command.hello (triggered by random_pet.this)

Apply complete! Resources: 1 added, 0 changed, 0 destroyed. Actions: 1 invoked.

=== SECTION 2: Force recreate ===
random_pet.this: Refreshing state... [id=amusing-bedbug]

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
-/+ destroy and then create replacement

Terraform will perform the following actions:

  # random_pet.this will be replaced, as requested
-/+ resource "random_pet" "this" {
      ~ id        = "amusing-bedbug" -> (known after apply)
        # (2 unchanged attributes hidden)
    }

    # Actions to be invoked after this change in order:
    action "local_command" "hello" {
        config {
            arguments = [
                "Hello from Terraform Actions!",
            ]
            command   = "echo"
        }
    }


Plan: 1 to add, 0 to change, 1 to destroy. Actions: 1 to invoke.
random_pet.this: Destroying... [id=amusing-bedbug]
random_pet.this: Destruction complete after 0s
random_pet.this: Creating...
random_pet.this: Creation complete after 0s [id=amazing-joey]
Action started: action.local_command.hello (triggered by random_pet.this)
Action action.local_command.hello (triggered by random_pet.this): 

Hello from Terraform Actions!


Action complete: action.local_command.hello (triggered by random_pet.this)

Apply complete! Resources: 1 added, 0 changed, 1 destroyed. Actions: 1 invoked.

=== SECTION 3: Invoke standalone ===

Terraform will perform the following actions:

Plan: 0 to add, 0 to change, 0 to destroy. Actions: 1 to invoke.

Terraform will invoke the following action(s):

  # action.local_command.hello will be invoked
    action "local_command" "hello" {
        config {
            arguments = [
                "Hello from Terraform Actions!",
            ]
            command   = "echo"
        }
    }

Action started: action.local_command.hello (triggered by CLI)
Action action.local_command.hello (triggered by CLI): 

Hello from Terraform Actions!


Action complete: action.local_command.hello (triggered by CLI)

Apply complete! Resources: 0 added, 0 changed, 0 destroyed. Actions: 1 invoked.

